const { CapsLockReplyStrategy } = require('./capslock_reply_strategy');
const { NightTimeReplyStrategy } = require('./nighttime_reply_strategy');
const { SentanceBasedReplyStrategy } = require('./sentance_based_reply_strategy');

module.exports = {
  CapsLockReplyStrategy,
  NightTimeReplyStrategy,
  SentanceBasedReplyStrategy,
};
